<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="unsw.infs.jingdianli.recipes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SearchResultDetailsActivity"></activity>
        <activity android:name=".activities.SearchResultsActivity" />
        <activity android:name=".activities.RecipeDetailsActivity" />
        <activity android:name=".activities.AllRecipesActivity" />
        <activity
            android:name=".quiz.QuizTestActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".quiz.QuizResultsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".quiz.QuizReviewActivity"
            android:screenOrientation="portrait" />

        <activity android:name=".activities.MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>